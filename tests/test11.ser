include "./ztring.ser";

let greg = "bruh\n";

fn main() {
  let s = "Test\n";
  let f = "Hello World\n";

  let st = string(s);
  printString(st);

  st = string(f);
  printString(st);

  st = string(greg);
  printString(st);
}